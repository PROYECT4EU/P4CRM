# Application source

This directory will contain the P4CRM application code.

The v0.1 milestone defines the data contract and workflows before selecting the final production runtime. Code added here must treat the schemas in `/schemas` and controlled values in `/config` as the repository source of truth.

Do not embed operational contact data, credentials or consent evidence in source files.
